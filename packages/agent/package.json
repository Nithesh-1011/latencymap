{
  "name": "@latencymap/agent",
  "version": "1.0.0",
  "description": "Cloudflare Worker probe deployed to 300+ edge locations",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}